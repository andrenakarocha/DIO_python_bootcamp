def definindo_tuplas ():
    frutas = ('laranja', 'pera', 'uva',)
    letras = tuple('python')
    numeros = tuple([1, 2, 3, 4])
    pais = ('Brasil',)

# Tuplas são usadas quando você quer uma garantia que o conteúdo dentro da mesma não seja alterado, mas o acesso e index é idêntico ao da lista.
